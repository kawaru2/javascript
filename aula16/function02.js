function soma(n1 = 0, n2 = 0) { // Estou colocando um valor = 0 para ser o meu parâmetro pré definido, caso eu não dê a informação, será considerado 0;
    return n1 + n2; // Minha ação, o meu bloco de código;
}
console.log(soma(2, 5)); // Minha chamada com o console e os parâmetros;